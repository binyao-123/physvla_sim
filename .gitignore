__pycache__/
*.py[cod]

*.hdf5
*.mp4
collect/datasets/
collect/logs/

${data}/
collect/${data}/

# ---- Local-only / unpublished (keep clones to Python-only; assets & tasks stay on your machine)
assets/
tasks/

# LeRobot dataset exports (large video/parquet; host via Hub or storage)
convert_data/lerobot_datasets/

# Pi05 sim rollout outputs (MP4 + summary.json)
rollout_sim/rollouts/
